defmodule Teenager do
  def hey(input) do
    cond do
        String.upcase(input) != input && !String.ends_with? input, "?" -> "Whatever."
        String.contains(intput, )
        String.last(input) == nil -> "Fine. Be that way!"
        String.upcase(input) == input -> "Whoa, chill out!"
        String.ends_with? input, "?" -> "Sure."
    end
  end
end
